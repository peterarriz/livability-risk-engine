version: 1
settings:
  actionable_statuses:
    - ready
    - in_progress
  dependency_ready_statuses:
    - ready
    - in_progress
    - done
  blocked_statuses:
    - blocked
lanes:
  product:
    display_name: Product
    ready_queue_target: 3
    current_focus:
      - product-008
    blockers: []
    update_when:
      - A product task changes status.
      - Product adds or removes acceptance criteria.
      - Product changes lane priorities or blocks another lane.
    notes: All prior product tasks are done. product-008 (launch-readiness questions) is now ready and is the next actionable product task.
  data:
    display_name: Data
    ready_queue_target: 3
    current_focus:
      - data-010
      - data-011
      - data-012
    blockers: []
    update_when:
      - A data ingestion or normalization task changes status.
      - A dependency from Product or App blocks data delivery.
      - Data decides the next source or schema checkpoint.
    notes: data-005 through data-009 and data-013 are done. Next focus is freshness checks (data-010), daily refresh docs (data-011), and normalization QA (data-012).
  app:
    display_name: App
    ready_queue_target: 3
    current_focus: []
    blockers: []
    update_when:
      - An app task changes status.
      - App is waiting on Data output or Product contract decisions.
      - The demo flow needs a different next-ready task.
    notes: app-015 through app-018 completed. ScoreHero placeholder copy fixed, .env.example populated, backend/requirements.txt added, README smoke-check updated with /health and live mode steps. app-007 (map view) remains backlog. No actionable app tasks remain; generate new tasks when the next integration milestone is defined.
