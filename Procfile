# Procfile
# task: data-019
# Fallback process definition for platforms that read Procfile instead of railway.toml
# (Heroku, Railway legacy, Render).
web: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
